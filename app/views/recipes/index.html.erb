<div class="container">	
	<div class="row">
		<h1> MY Favorite Recipe List </h1>
		  <% @recipes.each do |recipe| %>		
			<div class="col-md-4">
				<%= link_to	 (image_tag(recipe.image.url(:medium), class: "img-rounded", size: '300x150')) %><br>
			<div class="labels"><%= link_to recipe.title,recipe %></div><br>
			</div>
		<% end %>
	</div><br>
</div>
